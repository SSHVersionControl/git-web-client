security:
    acl:
        connection: default
    encoders:
        FOS\UserBundle\Model\UserInterface: sha512

    role_hierarchy:
        ROLE_ADMIN:       ROLE_USER
        ROLE_SUPER_ADMIN: ROLE_ADMIN

    providers:
        fos_userbundle:
            id: fos_user.user_provider.username_email

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            pattern: ^/
            form_login:
                provider: fos_userbundle
                csrf_provider: security.csrf.token_manager
            #oauth:
                #resource_owners:
                #    github:           "/login/check-github"
                #login_path:        /apilogin
                #failure_path:      /apilogin

                #oauth_user_provider:
                    #this is my custom user provider, created from FOSUBUserProvider - will manage the
                    #automatic user registration on your site, with data from the provider (facebook. google, etc.)
                #    service: version_control.api_user_provider
            logout:       true
            anonymous:    true
            remember_me:
                key:      "%secret%"
                lifetime: 2592000
                path:     /
                domain:   ~
                user_provider: fos_userbundle

    access_control:
        - { path: ^/login$, role: IS_AUTHENTICATED_ANONYMOUSLY }
        #- { path: ^/register, role: IS_AUTHENTICATED_ANONYMOUSLY }
        #- { path: ^/resetting, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/admin/, role: ROLE_ADMIN }
        - { path: ^/, role: ROLE_USER }
        
vmelnik_doctrine_encrypt:  
    # Secret key for encrypt algorithm. All secret key checks are encryptor tasks only.
    secret_key:           893d453ere@r78hu%s)7x
    #  Default and only one encryptor is aes256. If you want to provide your own - set encryptor_class
    encryptor:            aes256 
    #  If you want, you can use your own Encryptor. Encryptor must implements EncryptorInterface interface
    #  Default: VMelnik\DoctrineEncryptBundle\Encryptors\AES256Encryptor
    #encryptor_class:      ~ 
    #  You can optionally provide a service as an encryptor instead of specifying a class.  The service 
    #  must implement EncryptorInterface.  You do not need to provide encryptor_class if you provide the service.
    #encryptor_service:    ~
    #  Now it's only one db driver is supported - orm
    db_driver:            orm 

#hwi_oauth:
    # list of names of the firewalls in which this bundle is active, this setting MUST be set
    #connect:
    #    account_connector: version_control.api_user_provider
    #firewall_name: main
    #fosub:
    #    username_iterations: 30
    #    properties:
    #        github: githubId
    #resource_owners:
    #    github:
    #        type:                github
    #        client_id:           b22364a603f32101e207
    #        client_secret:       0d4e305fae37d8d84a2ad4272bfe88308e00432d